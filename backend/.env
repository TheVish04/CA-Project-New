# Server Configuration
PORT=5000
CORS_ORIGIN=http://localhost:5173

# Database Configuration (SQLite in this case)
DATABASE_URL=sqlite:./database/database.sqlite
DATABASE_STORAGE=./database/database.sqlite

# Default Admin Credentials (Development Only)
# In production, use a secure password and hash it in the database
# Replace these admin credentials:
ADMIN_FULL_NAME=Admin User
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=Vishal15125@

# JWT Configuration for Authentication
JWT_SECRET=mynameisvishalmejari
JWT_EXPIRES_IN=1d

# Environment
NODE_ENV=development

# Logging (Optional)
LOG_LEVEL=debug

# API Rate Limiting (Optional, if implemented)
RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
RATE_LIMIT_MAX=100  # Max 100 requests per window

# Additional Security (Optional, for production)
# SESSION_SECRET=your_session_secret_here
# COOKIE_SECRET=your_cookie_secret_here